# Project 4: Building the Machine's Optic Nerve
# Core dependencies for both recognition paths

opencv-python>=4.8.0        # OpenCV: image I/O, preprocessing, cv2.dnn (MobileNet-SSD)
pytesseract>=0.3.10          # Python wrapper for Google's Tesseract OCR engine
numpy>=1.24.0                # Array / matrix operations (the IPO model backbone)
Pillow>=10.0.0                # Used only by sample_images/generate_samples.py

# NOTE: pytesseract requires the Tesseract OCR *binary* to be installed
# separately at the OS level (it is not a pip-installable engine).
#
#   Ubuntu / Debian : sudo apt-get install tesseract-ocr
#   macOS (brew)    : brew install tesseract
#   Windows         : https://github.com/UB-Mannheim/tesseract/wiki
#
# Verify with:  tesseract --version
